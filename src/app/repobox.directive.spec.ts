import { RepoboxDirective } from './repobox.directive';

describe('RepoboxDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoboxDirective();
    expect(directive).toBeTruthy();
  });
});
